services:
  web:
    build: .
    ports:
      - "8080:8080"
    environment:
      API_BASE_URL: ${API_BASE_URL:-https://api.openai.com/v1}
      API_KEY: ${API_KEY:-}
      API_MODEL: ${API_MODEL:-gpt-4o-mini}
    volumes:
      - ./games:/app/games
      - ./data:/app/data
